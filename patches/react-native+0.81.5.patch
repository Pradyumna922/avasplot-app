diff --git a/node_modules/react-native/Libraries/Animated/nodes/AnimatedObject.js b/node_modules/react-native/Libraries/Animated/nodes/AnimatedObject.js
index 990242a..c442cd6 100644
--- a/node_modules/react-native/Libraries/Animated/nodes/AnimatedObject.js
+++ b/node_modules/react-native/Libraries/Animated/nodes/AnimatedObject.js
@@ -10,12 +10,12 @@
 
 'use strict';
 
-import type {PlatformConfig} from '../AnimatedPlatformConfig';
-import type {AnimatedNodeConfig} from './AnimatedNode';
+import type { PlatformConfig } from '../AnimatedPlatformConfig';
+import type { AnimatedNodeConfig } from './AnimatedNode';
 
 import AnimatedNode from './AnimatedNode';
 import AnimatedWithChildren from './AnimatedWithChildren';
-import {isValidElement} from 'react';
+import { isValidElement } from 'react';
 
 const MAX_DEPTH = 5;
 
@@ -23,12 +23,13 @@ export function isPlainObject(
   value: mixed,
   /* $FlowIssue[incompatible-type-guard] - Flow does not know that the prototype
      and ReactElement checks preserve the type refinement of `value`. */
-): value is $ReadOnly<{[string]: mixed}> {
+): value is $ReadOnly<{ [string]: mixed }> {
+  if (value === null || typeof value !== 'object') {
+    return false;
+  }
+  const proto = Object.getPrototypeOf(value);
   return (
-    // $FlowFixMe[incompatible-type-guard]
-    value !== null &&
-    typeof value === 'object' &&
-    Object.getPrototypeOf(value).isPrototypeOf(Object) &&
+    (proto === null || proto === Object.prototype || proto.isPrototypeOf(Object)) &&
     !isValidElement(value)
   );
 }
@@ -69,7 +70,7 @@ function mapAnimatedNodes(value: any, fn: any => any, depth: number = 0): any {
   } else if (Array.isArray(value)) {
     return value.map(element => mapAnimatedNodes(element, fn, depth + 1));
   } else if (isPlainObject(value)) {
-    const result: {[string]: any} = {};
+    const result: { [string]: any } = {};
     const keys = Object.keys(value);
     for (let ii = 0, length = keys.length; ii < length; ii++) {
       const key = keys[ii];
@@ -161,7 +162,7 @@ export default class AnimatedObject extends AnimatedWithChildren {
     return {
       type: 'object',
       value: mapAnimatedNodes(this._value, node => {
-        return {nodeTag: node.__getNativeTag()};
+        return { nodeTag: node.__getNativeTag() };
       }),
       debugID: this.__getDebugID(),
     };
